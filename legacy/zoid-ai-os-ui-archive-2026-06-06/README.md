# Zoid AI OS archive

Archived on 2026-06-06 before resetting the app to the clean Zoid 25 scaffold.

## Layout

- `frontend/src/` — previous React UI, view models, UI tests, and frontend bridge integrations.
- `frontend/dist/` — previous built frontend output.
- `native/src/` — previous Rust/Tauri backend source files.
- `native/migrations/` — previous SQLite/database migration SQL files.
- `docs/` — previous product/design/module docs.
- `hermes/` — previous implementation plans, screenshots, and critique/review artifacts.
- `tooling/` — previous generated/workflow helper folders.
- `data/backups/` — previous SQLite backup files.

The live app root now keeps only the clean scaffold files needed to start Zoid 25.
